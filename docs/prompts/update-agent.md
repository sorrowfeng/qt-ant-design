请更新 qt-ant-design 项目根目录下的 AGENT.md 文件，更新完成后提交并推送到远端仓库。

**更新要求：**

1. **读取当前代码状态**
   - 扫描 `src/widgets/` 目录，列出所有已实现的组件类（以 Ant 开头）。
   - 对照 Ant Design 官方组件列表（参考 [ant-design本地路径]/components/），识别哪些常用组件尚未实现。

2. **本次主要变更说明**
   [在此描述你做了哪些改动，例如：
   - 新增了 AntMenu 组件
   - AntTheme 增加了字体 token
   - 示例程序左侧导航改用 AntMenu
   ]

3. **更新 AGENT.md 以下章节**
   - **“已移植组件”**（或“已完成组件”）：
      - 列出当前已实现并可用的组件，包含简要说明和支持的主要属性/功能。
      - 如有必要，可分组展示（通用、布局、导航、数据录入、数据展示、反馈等），参考 Ant Design 的分类方式。
   - **“待移植组件”**（或“计划中组件”）：
      - 列出 Ant Design 常用但尚未实现的组件，按优先级排序。
      - 可使用复选框格式，便于追踪进度，例如：
        - [ ] AntMenu
        - [x] AntButton
   - **“开发规范”**：
      - 如果本次变更涉及新的设计模式或使用约定，请补充说明。
   - **“示例程序”**：
      - 说明当前 example 中展示了哪些组件，以及如何运行。

4. **输出与提交**
   - 输出完整的 AGENT.md 内容（覆盖原文件）。
   - 执行 Git 命令：
     ```bash
     git add AGENT.md
     git commit -m "docs: update AGENT.md - [简要描述本次更新内容]"
     git push
